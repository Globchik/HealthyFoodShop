<UserControl x:Name="statistics_page" x:Class="HealthyFood_Shop.MenuParts.StatisticsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HealthyFood_Shop.MenuParts"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1300" Loaded="statistics_page_Loaded">
    <Grid  Background="{DynamicResource MyYellowBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition  />
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="7*"/>
            </Grid.RowDefinitions>

            <Grid HorizontalAlignment="Left" VerticalAlignment="Center" >
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Border Grid.RowSpan="2" Background="{StaticResource MyLessBlack}" />
                <Label FontSize="30" Content="Переглянути статистичні дані:"/>
                <ComboBox Style="{DynamicResource cb_default}" x:Name="cb_plot_type"
                          FontFamily="Tahoma" FontSize="22" Height="35" Width="500"
                          Grid.Row="1" Margin="8 0 8 8" SelectionChanged="cb_plot_type_SelectionChanged" />
            </Grid>


            <ScottPlot:WpfPlot x:Name="wpfplot_sales_per_day" Grid.Row="1"/>
        </Grid>
        
    </Grid>
</UserControl>
