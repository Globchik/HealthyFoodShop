<Window x:Class="HealthyFood_Shop.GeneralWindows.LoginPassWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HealthyFood_Shop.GeneralWindows"
        mc:Ignorable="d" x:Name="login_pass_window"
        Title="Авторизація" Width="300" Height="320" ResizeMode="NoResize" 
        Loaded="login_pass_window_Loaded" WindowStartupLocation="CenterScreen" >
    <Grid Background="{DynamicResource MyYellowBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.4*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid Margin="15 0" Height="90" Grid.Column="1" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border Grid.RowSpan="2" Background="{StaticResource MyLessBlack}" />
            <Label x:Name="l_login" FontSize="25" Content="Логін"/>
            <TextBox Grid.Row="1"  x:Name="tb_login" Style="{StaticResource tb_default}"
                         Text="" FontSize="25" Margin="8 0 8 8" Padding="2"/>
        </Grid>
        <Grid Margin="15 0" Height="90" Grid.Column="1" Grid.Row="2" >
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border Grid.RowSpan="2" Background="{StaticResource MyLessBlack}" />
            <Label x:Name="l_pass" FontSize="25" Content="Пароль"/>
            <TextBox Grid.Row="1"  x:Name="tb_pass" Style="{StaticResource tb_default}"
                         Text="" FontSize="25" Margin="8 0 8 8" Padding="2"/>
        </Grid>
        <Button x:Name="btn_apply" Grid.Row="3" Grid.Column="1"
                Style="{StaticResource btn_black_text}"
                Content="✔" Margin="20 0" Click="btn_apply_Click"/>
    </Grid>
</Window>
